---
sources: [summaries/4A3网络实施.md, summaries/19.3.1数据存储.md, summaries/16.4.5 Web应用系统部署.md, summaries/15.7.2数据转换和迁移.md, summaries/15.7.1新旧系统的转换策略.md]
brief: 系统转换是将现有系统平滑过渡到新系统的关键阶段，包括直接转换、并行转换和分段转换三种策略，以及完整的数据迁移流程。
---

# 系统转换

系统转换（System Conversion）是指将现有系统平滑过渡到新系统的过程，是[[信息系统开发]]项目中的关键阶段。系统转换本质上也是一个[[工程项目]]，需要进行[[可行性分析]]和[[需求分析]]，并制订详细的[[项目管理计划]]。

系统转换不仅涉及策略选择，还包含数据转换和迁移的核心环节。数据迁移对系统切换乃至新系统的运行有着十分重要的意义，其质量是新系统成功上线的重要前提，同时也是新系统今后稳定运行的有力保障。

## 转换场景

系统转换可分为两种主要场景：

1. **升级替换**：功能更强大的新系统取代现有系统
   - 网络作业系统取代单机系统
   - 开放性系统取代封闭性系统
   - 标准化的系统取代不规范的系统
   - 更安全的系统取代不太安全的系统
   - 系统功能与性能的提高

2. **手工替代**：计算机信息系统取代原有的手工作业系统

## 三种转换策略

### 直接转换策略

在原有系统停止运行的某一时刻，新系统立即投入运行，中间没有过渡阶段。

- **优点**：人力和费用最省
- **缺点**：风险较高
- **适用场景**：新系统不太复杂或现有系统完全不能使用的场合

### 并行转换策略

新系统和现有系统并行工作一段时间，经过试运行后再切换。

并行转换的实施过程中，首先以现有系统的作业为正式作业，新系统的处理结果作为校核之用，经过一段时间的运行，在验证新系统处理准确可靠后，现有系统退出运行。根据系统的复杂程度和规模大小不同，并行运行的时间一般可在两三个月到一年之间。

并行转换的优点是风险较小，在转换期间可同时比较新旧两个系统的性能，并让系统操作员和其他有关人员得到全面培训。缺点是人力和费用消耗较大，转换周期长，难以控制新旧系统中的数据变化。这就要求做好转换计划并加强管理，在新旧系统验证吻合后要及时停止现有系统的运行。

### 分段转换策略

分段转换策略也称为逐步转换策略，采取分期分批逐步转换。这种转换方式是直接转换方式和并行转换方式的结合，一般比较大的系统采用这种转换方式较为适宜，它能保证平稳运行，费用也不太高。

分段转换的具体策略包括：

- **按功能分阶段逐步转换**：首先确定新系统中的一个主要的业务功能率先投入使用，在该功能运行正常后再逐步增加其他功能
- **按部门分阶段逐步转换**：先选择系统中的一个合适的部门，在该部门运行新系统，获得成功后再逐步扩大到其他部门
- **按机器设备分阶段逐步转换**：先从简单的设备开始转换，再推广到整个系统

分段转换的优点是新旧系统的转换震动比较小，用户容易接受。但由于是采用渐进方式，导致新旧系统的转换周期过长，同时由于需求的变化，给新系统的稳定造成比较大的影响。

## 数据转换与迁移

数据转换和迁移是系统转换交接的主要工作之一。为使数据能平滑迁移到新系统中，在新系统设计阶段就要尽量保留现有系统中合理的数据结构，这样才能尽可能降低数据迁移的工作量和转换难度。

### 迁移原则

数据转换和迁移工作的原则是**数据不丢失**。许多无法自动转换的数据，必要时通过手工方式补录进入新系统。成功的数据迁移可以有效地保障新系统的顺利运行，而且能够继承珍贵的历史数据；反之，若迁移质量较差，没能屏蔽全部的垃圾数据，对新系统将会造成很大的隐患。

### 三种迁移方法

1. **系统切换前通过工具迁移**：利用[[ETL工具]]把现有系统中的历史数据抽取、转换，并装载到新系统中去。这种方法是数据迁移最主要，也是最快捷的方法。这种迁移方式既可一次实现，也可以分次实现。

2. **系统切换前采用手工录入**：组织相关人员把需要的数据手工录入到新系统中。这种方法消耗的人力、物力比较大，同时出错率也比较高。主要针对新旧系统数据结构存在特定差异的情况。

3. **系统切换后通过新系统生成**：通过新系统的相关功能，或为此专门开发的配套程序生成所需要的数据。通常根据已经迁移到新系统中的原始数据来生成所需要的结果数据。

### 迁移前的准备工作

数据迁移的实施可以分为三个阶段：数据迁移前的准备、数据转换与迁移和数据迁移后的校验。充分而周到的准备工作是完成数据迁移的主要基础，具体需要做好以下工作：

1. 待迁移数据源的详细说明，包括数据的存放方式、数据量和数据的时间跨度
2. 建立新旧系统数据库的数据字典，对现有系统的历史数据进行质量分析，以及新旧系统数据结构的差异分析
3. 新旧系统代码数据的差异分析
4. 建立新旧系统数据库表的映射关系，以及对无法映射字段的处理方法
5. 开发或购买、部署ETL工具
6. 编写数据转换的测试计划和校验程序
7. 制定数据转换的应急措施

### 迁移流程

数据转换与迁移程序大致可以分为抽取、转换与装载三个过程。数据抽取、转换是根据新旧系统数据库的映射关系进行的，转换步骤一般还要包含[[数据清洗]]的过程。数据清洗主要是对源数据库中出现二义性、重复、不完整、违反业务或逻辑规则等问题的数据进行相应的清洗操作。

数据抽取前需要做大量的准备工作：针对目标数据库中的每张数据表建立抽取函数，根据SQL语句进行优化，建立调度控制表（ETL函数定义表、抽取调度表、装载调度表、抽取日志表、装载日志表），以及建立调度控制程序。

### 迁移后的校验

数据迁移后的校验是对迁移质量的检查，同时数据校验的结果也是判断新系统能否正式启用的重要依据。可以通过以下两种方式进行校验：

1. **对迁移后的数据进行质量分析**：检查指标包括完整性检查、一致性检查、总分平衡检查、记录条数检查和特殊样本数据的检查
2. **新旧系统查询数据对比检查**：通过新旧系统各自的查询工具，对相同指标的数据进行查询，并比较最终的查询结果

## 关键考虑因素

由于现有系统往往积累了大量珍贵的历史数据，系统转换计划应包括从现有系统向新系统的[[数据迁移]]计划，尽量减小在新旧系统转换时对用户现有业务的冲击。此外还需要考虑维持新系统运行的日常开销、由于使用习惯改变带来的学习时间，以及培训人员的成本。

---
**参考来源**：[[summaries/15.7.1新旧系统的转换策略]]、[[summaries/15.7.2数据转换和迁移]]

See also: [[summaries/16.4.5 Web应用系统部署]]

See also: [[summaries/19.3.1数据存储]]

See also: [[summaries/4A3网络实施]]